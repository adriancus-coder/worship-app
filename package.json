{
  "name": "worship-app",
  "version": "0.1.0",
  "private": true,
  "description": "Worship team app: songs, events and live projection for churches.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check-syntax.js && node scripts/check-i18n.js && node scripts/check-contrast.js && node scripts/test-lib.js && node scripts/test-live.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "compression": "^1.8.2",
    "dotenv": "^18.0.3",
    "express": "^4.22.3",
    "fast-xml-parser": "^5.11.1",
    "helmet": "^8.3.0",
    "sharp": "^0.35.4",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.4"
  }
}
